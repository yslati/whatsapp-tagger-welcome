export interface Stat {
  icon: string;
  value: string;
  label: string;
}

export type Feature = {
  icon: string
  title: string
  description: string
  stats: string
}

export type Step = {
  icon: string;
  gradient: string;
  title: string;
  description: string;
}
